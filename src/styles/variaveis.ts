export default {
  azulEscuro: '#74b9ff',
  azulClaro: '#bddeff',
  cinza: '#eee',
  cinzaEscuro: '#666666',
  branco: '#fff'
}

const breakpoints = {
  md: '767px',
  lg: '1023px'
}

export const devices = {
  md: `(max-width: ${breakpoints.md})`,
  lg: `(max-width: ${breakpoints.lg})`
}
